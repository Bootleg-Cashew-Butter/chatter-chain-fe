import React, { Component } from 'react'
import styles from './ActiveUserList.styl'

export default class ActiveUserList extends Component {
  render () {
    return <div className={styles.element}>
      Active User List
    </div>
  }
}
